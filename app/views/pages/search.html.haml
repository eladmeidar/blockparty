.row
    .col-sm-12
        .page-header
            %h1 
                Search results for
                = params[:q]
                %small
                    = pluralize(@users.count, 'result')
    .col-sm-12
        = render partial: 'shared/profile', collection: @users, as: :user